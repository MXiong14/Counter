// first we need to set the inital value to zero
let count = 0;
// we can now setup functionality for our buttons on HTML page
// we use #value in our first querySelector because we want select the first element with the id of "value"
const value = document.querySelector("#value");

